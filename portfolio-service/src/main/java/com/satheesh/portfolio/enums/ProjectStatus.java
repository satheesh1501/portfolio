package com.satheesh.portfolio.enums;

/**
 * Publication status for portfolio projects.
 * ACTIVE      → Live and publicly visible.
 * IN_PROGRESS → Currently being built (e.g., Civil Platform).
 * ARCHIVED    → Retired or legacy project.
 */
public enum ProjectStatus {
    ACTIVE,
    IN_PROGRESS,
    ARCHIVED
}
